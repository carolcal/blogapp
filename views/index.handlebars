<div class="bg-light p-5 rounded-lg">
    <h1 class="display-4">Welcome to my blog!</h1>
    <p class="lead">This is a blog developed with Node.js + Express + MongoDB.</p>
    <hr class="my-4">
    <p> Here I will post things I've learned in my journey in development.</p>
</div>

<hr>

<h2>Recent posts: </h2>
{{#each posts}}
<div class="card mt-4">
    <div class="card-body">
        <h4>{{title}}</h4>
        <p>{{description}}</p>
        <a href="/post/{{slug}}">
            <button class="btn btn-success">Read More</button>
        </a>
        <hr>
        <small>Category: {{category.name}}</small>
        <br>
        <small>Publish Date: {{date}}</small>
    </div>
</div>
{{else}}
<h5 class="mt-3">No post registered.</h5>
{{/each}}