<div class="bg-light p-5 rounded-lg">
    <h1 class="display-4">Bem-vindo ao blog do Node</h1>
    <p class="lead">Este é um blog simples desenvolvido com Node.js + Express + MongoDB.</p>
    <hr class="my-4">
    <p>Aqui você poderá ler postagens sobre programação.</p>
    <a class="btn btn-primary btn-lg" href="#" role="button">Crie uma conta</a>
</div>

<hr>

<h2>Postagens recentes: </h2>
{{#each postagens}}
<div class="card mt-4">
    <div class="card-body">
        <h4>{{titulo}}</h4>
        <p>{{descricao}}</p>
        <a href="/postagem/{{slug}}">
            <button class="btn btn-success">Leia Mais</button>
        </a>
        <hr>
        <small>Categoria: {{categoria.nome}}</small>
        <br>
        <small>Data de publicação: {{date}}</small>
    </div>
</div>
{{else}}
<h5 class="mt-3">Nenhuma postagem registrada.</h5>
{{/each}}